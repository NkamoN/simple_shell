#include "main.h"

/**
 * handle_hex - function
 * @num: num input
 * @is_upper: 0 or 1
 * @count: count
 */
void handle_hex(unsigned int num, int is_upper, size_t *count)
{
	print_hex(num, is_upper, count);
}
